//Connect to the server the variable client has the connection string
client.connect()

//Create a cluster or database
const db = client.db(mycluster)

//A collection names people is created
const col = db.collection("people")

//A document is the type of data that can be inserted in the database of a mongodb client
 let personDocument = {
             "name": { "first": "Alan", "last": "Turing" },
             "birth": new Date(1912, 5, 23), // May 23, 1912                                                                                                                                 
             "death": new Date(1954, 5, 7),  // May 7, 1954                                                                                                                                  
             "contribs": [ "Turing machine", "Turing test", "Turingery" ],
             "views": 1250000
            }

// Insert the document into the specified collection        
         const p = col.insertOne(personDocument);

// Find and return the document-Retrieve data
         const filter = { "name.last": "Turing" };
         const document = await col.findOne(filter);
         console.log("Document found:\n" + JSON.stringify(document));
         